# Common values for all releases
# Add any shared configuration here

# Development environment

imageTag: latest

replicaCount: 2

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "200m"
