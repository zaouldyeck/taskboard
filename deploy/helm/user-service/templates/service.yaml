apiVersion: v1
kind: Service
metadata:
  name: taskboard-user-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
  - port: 50052
    targetPort: 50052
    protocol: TCP
    name: grpc
  selector:
    app: user-service
